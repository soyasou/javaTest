package com.hapt.hapt.controllers;

import org.springframework.stereotype.Controller;

@Controller
public class AddUser {
    // ** @Autowired
    // * UserRepository userRepository;

    // * @RequestMapping("/user")
    // * @ResponseBody
    // * public String create(String name, String prenom, String email, String
    // password, String role, String adresse,
    // * String niveau, int active) {
    // * String userId = "";
    // * name = "souad";
    // * prenom = "ALOUI";
    // * email = "souadaloui104@gmail.com";
    // * password = "$2a$10$XptfskLsT1l/bRTLRiiCgejHqOpgXFreUnNUa35gJdCr2v2QbVFzu";
    // * role = "admin";
    // * adresse = "Rue 12ala 20154";
    // * niveau = "bac+5";
    // * active = 1;
    // * try {
    // * User user = new User(name, prenom, email, password, role, adresse, niveau,
    // active);
    /**
     * userRepository.save(user); userId = String.valueOf(user.getId()); } catch
     * (Exception ex) { return "Error creating the user: " + ex.toString(); } return
     * "User succesfully created with id = " + userId; }
     * 
     */
}
